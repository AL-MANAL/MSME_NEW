@using System.Data;
@model IEnumerable<ISOStd.Models.ProductShareBarchart>
@{
    //DataSet data1=ViewBag.dsData;
    ViewBag.Title = "BarChart";
    //var db = Database.Open("Data Source=localhost;Initial Catalog=isosoft;User Id=root;Password=manal@123;");
    //var data = //db.Query("select Reason_for_Delay, count(Reason_for_Delay)  from t_prod_log where Reason_for_Delay <> '' group by Reason_for_Delay");
    if(Model.Count() > 0)
    {
    var myChart = new Chart(width: 400, height: 200, themePath: "~/content/chart/chartthemeLostOrder.xml")
      .AddTitle("Short Fall Source")
      .AddSeries(name: "Product", axisLabel: "Product")
       .DataBindTable(Model, "Product")
      .Write();
    }
}

